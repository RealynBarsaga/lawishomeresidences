RewriteEngine On
RewriteBase /admin/

# Redirect root to login.php
RewriteRule ^$ login.php [L]

# Block direct access to login.php if explicitly requested
RewriteCond %{THE_REQUEST} ^[A-Z]{3,9}\ /admin/login\.php\ HTTP/ [NC]
RewriteRule ^login\.php$ /admin/ [R=302,L]