RewriteEngine On

# Redirect /admin to login.php with the pages=login parameter
RewriteRule ^admin/?$ /admin/login.php?pages=login [L,QSA]

# Optional: Force redirect without showing /login.php in the URL
RewriteCond %{REQUEST_URI} ^/admin/login.php$
RewriteRule ^admin/?$ /admin [R=301,L]